generates:
  ./src/typings/graphql-schemas.d.ts:
    schema: 'src/node/**/*.graphql'
    plugins:
      - typescript
    config:
      enumsAsTypes: true
hooks:
  afterOneFileWrite:
    - prettier --write
